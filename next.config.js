/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'acropolis-wp-content-uploads.s3.us-west-1.amazonaws.com'],
  },
}

module.exports = nextConfig 